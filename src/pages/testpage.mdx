---
title: Testpage
---

export var x = 123;

export default XXXX;
import ReactDOM from 'react-dom';
var listOfImages =[];
var listOfImages2 =[];


export function importAll(r) {
  let images = {};
  r.keys().map((item, index) => { images[item] = r(item); });
  return images;
}


import Logo from "/img/prider.png";
<img src={Logo}/>

export class XXXX extends React.Component{
    importAll(r) {
        return r.keys().map(r);
    }
    componentWillMount() {
        listOfImages = this.importAll(require.context('/img/', false, /\.(png|gif|jpe?g|svg|ico)$/));
        listOfImages2 = this.importAll(require.context('/img/', false, /\.(png|gif|jpe?g|svg|ico)$/));
    }
    render(){
        return(
          <div>
            <img src={Logo}/>
          <div> 
              {
                    listOfImages.map(
                      (image, index) =>    <div><img key={index} src={"./"+image} alt={index+": " + image}></img><br></br><img alt={Logo}/></div>
                    )
              },
               {
                    listOfImages2.map(
                      (image, index) =>    <div><img key={index} src={"./"+image} alt={index+": " + image}></img><br></br><img alt={Logo}/></div>
                    )
              },
              
            </div>
        </div>
        )
    }
    
}
